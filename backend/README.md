# Receipt Manager Backend

This folder contains the FastAPI backend for Receipt Manager.

It handles authentication, SQLite account and receipt storage, receipt validation, tax-summary calculation, optional Gemini OCR extraction, AI chat responses, admin endpoints, and PDF generation.

For full setup, Docker, deployment, and evaluation instructions, see the root project README:

```text
../README.md
```
